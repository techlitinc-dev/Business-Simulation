# Day 19 — Test Specification

## Frontend Tests
1. `CopilotPanel renders chat bubble icon when closed`
2. `CopilotPanel opens on click, shows empty state`
3. `CopilotPanel sends message and shows response`
4. `ChatBubble shows grounded badge for assistant messages`
5. `ChatBubble shows flagged claim warning`
6. `DecisionCoach shows second opinion button`
7. `DecisionCoach calls sendCopilotMessage with option label`

## Run Commands
```bash
cd frontend && npm run build && npm run lint
```

## Expected
```
Build: 0 errors
Lint: 0 errors
```
